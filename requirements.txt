[tool.pytest]
DJANGO_SETTINGS_MODULE = "myproject.settings"

[tool.black]
line-length = 120

[tool.isort]
profile = "black"

[tool.coverage.run]
source = ["myproject"]

[tool.coverage.report]
show_missing = true

[tools.sphinx]
html_theme = "sphinx_rtd_theme"

[tool.django-stubs]
django_settings_module = "myproject.settings"

[tool.dephell.main]
from = {format = "poetry", path = "pyproject.toml"}
to = {format = "pip", path = "requirements.txt"}

[tool.dephell.poetry.virtualenvs]
create = true
in-project = true

[tool.dephell.poetry]
version = "0.1.0"

[tool.dephell.poetry.dependencies]
django = "==4.2.9"
python-dotenv = "==1.0.1"
django-stubs-ext = "==1.20.0"
django-material = "==1.9.0"
whitenoise = "==6.6.0"
gunicorn = "==21.2.0"
psycopg2-binary = "==2.9.5"
django-mysql = "==4.21.0"
pytest = "==7.2.0"
pytest-django = "==4.5.2"
coverage = "==7.2.1"
flake8 = "==6.0.0"
black = "==23.3.0"
isort = "==5.12.0"
sphinx = "==6.1.3"
sphinx-rtd-theme = "==1.2.0"
ipython = "==8.14.0"

[tool.dephell.poetry.dev-dependencies]
pytest-cov = "==4.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
